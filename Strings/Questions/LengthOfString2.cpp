#include <iostream>
#include <string>  // Required for std::string
using namespace std;

int main() {
    string S = "welcome";
    cout << "Length (C++): " << S.length() << endl;  // Output: 7 and we can also use s.size()
    return 0;
}